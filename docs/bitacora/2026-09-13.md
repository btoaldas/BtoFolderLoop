# 2026-09-13

Initialized an independent desktop utility with GPL licensing, two cleanup modes, explicit preview approval and native Trash. Defined module boundaries and synthetic validation before implementation.

- Applied the personal-project naming convention: BtoFolderLoop. One repository and one directory; retained the initial history.
- Implemented four Swift targets with core ports, a native adapter, SQLite migrations and SwiftUI presentation. Added protection for link destinations found in the selected tree and for directories inside application packages.
- Validated 23 tests with native Trash enabled, zero failures. The 10,000-directory in-memory planning case took about 0.20 seconds; this is not a disk/cloud throughput claim.
- Completed a native UI approval/cancel/single-pass/cascade flow with generated fixtures; independently verified original hashes/identities and SQLite. Confirmed the saved mode after reopening.
- Fixed packaging to find the migration resource in the relocated app instead of a build directory. Stripped release debug metadata and confirmed the packaged binary does not contain a private home/build prefix. Final ARM64 ZIP is approximately 213 KiB, locally signed ad hoc and not notarized.
- Source check passed for 36 tracked files. No third-party packages or runtime network endpoints. Codebase index prepared locally; generated files, fixtures, private evidence and databases are excluded from Git.
- Registered the project in the local project inventory. Public GitHub publication was requested; repository owner confirmation remains the final external prerequisite.

## Public preview preparation

- Received authorization to compile, push and release at the proposed public destination `btoaldas/BtoFolderLoop`.
- Made release checksums portable and added source-commit build metadata. Documented Apple Silicon packaging and the absence of Apple notarization.
- Remote CI and a fresh download smoke test are required before marking publication complete.

## Public preview completed

- Published public repository `btoaldas/BtoFolderLoop` with `main` as the default branch and GPL detection. Remote HEAD matched the reviewed source commit.
- Published annotated tag and preview release `v0.1.0` from `b21e0a54d05b1c30c69d00212e98fdda4fe90af6`; uploaded the ARM64 ZIP, portable checksum and build metadata.
- Local 23-test suite passed with native Trash enabled. GitHub macOS 15 / Swift 6.1.2 CI passed on both the main push and tag push; it intentionally skips the opt-in native Trash case.
- Downloaded the published assets and verified the ZIP hash, local signature, resource migration and source commit. The downloaded app passed chooser/preview/cancel/single-pass/cascade/history tests on artificial data. Files kept their hashes and identities, links/packages/root were preserved, and three empty Trash destinations matched their original identities. SQLite integrity was `ok`.
- Reopened the downloaded app and confirmed the cascade preference remained selected without starting any cleanup. No user archive was changed. No global application installation or security setting was changed.
- Publication evidence and residual limits are recorded in the public milestone; raw local paths/databases remain excluded from Git.

## v0.2.0 selection and identity update

- Added a core selection model bound to one scan, default-off checkboxes, all/none controls and immutable approval of only the selected subset. The review sheet shows all marked paths, including filtered-out rows. Parents and children are never selected implicitly.
- Added nine core selection tests and one native partial-selection test. All 33 tests passed with native Trash enabled. Existing schema and saved settings remain compatible.
- UI verified: all unchecked at scan completion; select-all/none; filtering without changing selection; complete review list; cancel with zero runs; mode and re-scan reset; partial cascade preserves an unselected intermediate directory and its parent; single-pass moves only the chosen candidate.
- Independent filesystem/SQLite checks preserved all control file bytes and identities, unselected folders, protected package and symlink target. Two test runs journaled exactly the approved subsets.
- Generated an original raster icon with the built-in OpenAI image tool. Included source/provenance, ten native icon representations and branded main, confirmation, history and About views. Package and About show version 0.2.0.
- Published v0.2.0 from `f62613da5c6115de58737dddcb896b31341d2a01`. GitHub checks passed; download hash matched the uploaded asset. Verified signature and exact hashes of the installed executable, native icon, source image, metadata and migration against the GUI-tested package. Finder showed the native icon. No installation replacement was performed by the release workflow.

## v0.3.0 local history and retention work

- Observed an existing cleanup through short read-only SQLite transactions, following one run once per minute. Kept the installed application, active database, preferences and user folders untouched by the development workflow.
- Confirmed the previous summary counter is only written on completion. Added atomic live counters, last activity/finish time, folder aggregation and per-run event pagination.
- Added typed JSONL diagnostics without user paths, daily/size rotation and configurable retention: 30 days / 10 MiB diagnostics; 180 days completed operation history. Pending, cancelled, stopped, inconsistent and errored histories remain protected.
- Added a separate settings sheet and history views, with local dates, editable numeric fields and visible retention consequences. History can be inspected during work; settings and history expiration are blocked while work is active.
- Added schema 2 without modifying published schema 1. Online SQLite backup precedes migration. Synthetic legacy migration and a 20,000-event fixture preserved counters and records; history paging uses the run/id index without duplicating all path text in a second index.
- Validation: 47 Swift tests discovered, 45 passed, 2 native Trash tests intentionally skipped to avoid interfering with the active cleanup. Two Python observer tests passed, including missing-file protection and unchanged synthetic DB hash. Offscreen AppKit-hosted UI rendered and was visually inspected; initial ImageRenderer placeholders were rejected as inadequate evidence.
- New source remains local pending a separately authorized publication/installation stage. No live database was migrated or expired. Protected old journals and migration backups are intentionally outside diagnostic size limits.


## v0.3.0 signed updater and multi-folder input

- Added a footer with installed/public version, release notes and one-click verified installation/relaunch. Public preview discovery uses the release list, exact architecture/asset origins and numeric version comparisons. The real read-only catalog still reports public 0.2.0.
- Pinned Sparkle 2.9.6 and preserved framework layout/licensing. Required signed feeds and Ed25519 archive validation before extraction; kept the private key in Keychain. Update and folder-work guards prevent either operation from interrupting the other. Typed update diagnostics contain no private paths.
- Generated and verified the signed archive/feed locally. Exercised the production updater against synthetic app bundles: valid installation, an already-running fixture terminated/relaunched under a new PID, altered archive rejection and altered signed-feed rejection. Preserved external data sentinels. No user app or journal was used.
- Added several-folder input from the chooser/drop/Open Files path. Resolved explicit Finder aliases/symlinks to canonical real roots without altering shortcuts. Reported broken, cyclic, file/package and protected targets; internal-tree links remain untraversed.
- Added independent batch models/planner/selection/executor. Deduplicated identities/overlapping paths; protected every selected nested root and ancestor; kept all checkboxes off at scan completion. Global cancellation/error stops pending trees; SQLite keeps a separate journal per executed root.
- Passed core batch/alias/UI model tests. The opt-in native batch flow moved exactly three synthetic empty directories, verified original absence and Trash inode/device/emptiness, retained root/file identities and file/alias bytes, and produced two completed independent journals. Reusing the consumed approval caused no new execution.
- Added a scrollable work area with the footer outside the scrolling region. Offscreen native-control rendering covers resolved input, batch preview, complete approval and the update footer; the application identity is additionally checked in the packaged bundle.
- Documentation, maintenance/signing runbook and private key custody pointer prepared. The installed v0.2.0 application remains unchanged; v0.3.0 is local and neither pushed nor released at this stage.

Packaged runtime verification: launched an isolated app identifier and data directory, confirmed the embedded Sparkle framework loaded and schema 2 initialized with integrity ok. The actual native chooser selected two folders plus their Finder alias; the UI showed two unique roots, default-off candidates, select-all and a complete per-root confirmation. Cancelled without execution (zero runs in the independent DB), then closed only that test application. Finder drag gesture itself was not part of this UI check; its multiple-URL handler feeds the tested input pipeline.


## v0.3.0 publication and installation completed

- Received explicit approval for the prepared release and installation. Published reviewed source/tag `v0.3.0` at `4549913f28239020f3010ecc50e5949ec156a7e9`; macOS CI passed for both the source tag and main publication.
- Published the ARM64 archive, portable checksum, source metadata and signed appcast. Fresh public downloads matched the reviewed bytes, source commit and signatures. Published the identical signed architecture-specific feed only after those assets were available; verified its public raw response and signature.
- Confirmed the application was idle/closed. Created and verified backups of the old bundle and SQLite database before replacing the application with the freshly downloaded bundle. Preserved the original installed bundle as well.
- Opened installed v0.3.0 and confirmed the visible footer reported the latest public version. Verified schema 2 and schema-1 migration backup integrity; all pre-existing events, stable run fields and preferences matched the preinstallation snapshot. No folder scan/cleanup was started by the update.
- Historical local-preparation notes above describe their point in time. The current public/install state is recorded in the v0.3.0 public milestone; application signatures remain ad hoc without Apple notarization.
